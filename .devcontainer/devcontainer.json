{
  "name": "TariffRisk Dev Container",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  },
  "settings": {
    "terminal.integrated.defaultProfile.linux": "bash"
  },
  "extensions": [
    "ms-python.python",
    "ms-toolsai.jupyter",
    "esbenp.prettier-vscode"
  ],
  "postCreateCommand": "make install && make build && make test",
  "remoteUser": "vscode"
}
